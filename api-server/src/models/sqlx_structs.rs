// For intermediate SQLx structs, generably the prefered approach is to use
// the helium-types structs directly in a single query, however this may be
// complex if the query requires multiple joins or other complex logic.